<?php

	if ( ! defined( 'FW' )) {
		exit;
	}

/* 	$manifest = array();
	$manifest['id'] = 'FB';

	$manifest['supported_extensions'] = array(
		'sidebars'		=> array(),
		'breadcrumbs'	=> array(),
	); */
